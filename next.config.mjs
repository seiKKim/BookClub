/** @type {import('next').NextConfig} */
const nextConfig = {
    reactStrictMode: true,
    images: {
      domains: ['kr1-api-object-storage.nhncloudservice.com'],
    },
  };  

export default nextConfig;
